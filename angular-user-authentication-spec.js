
describe('angular-user-authentication ->', function() {

  beforeEach(module('davestone.userAuthentication'));

  it('foobar', function(done) {
    expect('foobar').to.equal('foobar');
    done();
  });

});
